# trm
A data compression format, which can be used to compress files and directories.

## How to use it

```bash
trm <file/dir>
```

-> This will create a `0.trm` file, if you want to compress more into one file:
```bash
trm <file0/dir0> <file1/dir1> <...>
```
